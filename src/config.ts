/**
 * Central configuration for review thresholds.
 * Nothing in the codebase may hardcode these values — always import from here.
 */

/** Minimum reviews before any scores are displayed for a company. */
export const MIN_REVIEWS_DISPLAY = 5;

/** Minimum reviews before scores are displayed for a sensitive identity lens. */
export const MIN_REVIEWS_SENSITIVE_LENS = 8;
